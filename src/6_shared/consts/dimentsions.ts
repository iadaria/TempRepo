import { Dimensions } from 'react-native';

export const HEIGHT = Dimensions.get('window').height; // or something else
export const WIDTH = Dimensions.get('window').width;

export const BOX_PADDING = 25;

export const GAP = 20;
