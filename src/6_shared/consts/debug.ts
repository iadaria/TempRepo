import { StyleSheet } from 'react-native';

export const debugStyles = StyleSheet.create({
  red: {
    borderWidth: 1,
    borderColor: 'red',
  },
  blue: {
    borderWidth: 1,
    borderColor: 'blue',
  },
});
